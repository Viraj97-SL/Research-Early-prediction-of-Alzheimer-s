# Core DL
torch>=2.0
monai==1.5.0          # do NOT pin numpy alongside this; 1.3/1.4 have Py3.12/numpy incompatibilities

# Medical imaging / IO
torchio>=0.19
nibabel>=5.0

# ML / metrics / tuning
scikit-learn>=1.3
imbalanced-learn>=0.11
optuna>=3.4

# Data / utils
pandas>=2.0
tqdm>=4.65

# Notes:
# - numpy is intentionally unpinned (let monai 1.5 resolve a compatible version).
# - HD-BET (skull-stripping) is installed separately per its own repo instructions.
